@page "/Test"
@using Microsoft.AspNetCore.Components.Web
@rendermode RenderMode.InteractiveServer
@using ReactLib

<Hello />

<Counter @bind-Value=i />
<input @bind=i @bind:event="oninput"/>
@code {
    int i;
}